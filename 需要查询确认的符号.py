as with as 语句的一部分
assert 断言（确保）某东西为真
class 定义类
del  从字典中删除
except 如果发生异常，运行此处代码
exec 将字符串作为python脚本运行
finally 不管是否发生异常，都运行此处代码
global 声明全局变量
is 类似于==，判断是否一样
lambda 创建短匿名函数
pass 表示空代码块
raise 出错后引发异常
return 返回值并退出函数
try 尝试执行代码，出错后转到except
with 将表达式作为一个变量，然后执行代码块
yield 暂停函数，返回调用函数的代码中

数据类型
bytes 字节串存储
dicts 存储键-映射值
